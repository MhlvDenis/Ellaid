version: "3.7"

services:
  app:
    container_name: comment-service
    build: .
    restart: always
    networks:
      - comment-service-net
    ports:
      - "8080:8080"
    depends_on:
      - mongodb

  mongodb:
    container_name: comment-mongodb
    image: mongo:latest
    restart: always
    networks:
      - comment-service-net
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

networks:
  comment-service-net:

volumes:
  mongodb_data:
